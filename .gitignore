# Python
__pycache__/
*.py[cod]
.venv/
venv/
.ipynb_checkpoints/

# Raw AIID export snapshots can be large — archive them out-of-band and
# record the access date in METHODOLOGY.md instead of committing them.
data/raw/*
!data/raw/.gitkeep
